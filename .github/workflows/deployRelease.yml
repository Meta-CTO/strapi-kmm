name: Build & upload the debug package
# Trigger on every tag creation
on:
  push:
    branches:
      - 'release'
      - 'develop'
      - 'auth_kmm'
      - 'v*'
# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - name: set up JDK 11
        uses: actions/setup-java@v1
        with:
          java-version: 11
      - name: Cache Gradle
        uses: actions/cache@v2
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-
      - name: Cache Konan
        uses: actions/cache@v2
        with:
          path: |
            ~/.konan/caches
            ~/.konan/dependencies
            ~/.konan/kotlin-native-prebuilt-macos-*
          key: ${{ runner.os }}-konan-${{ hashFiles('**/Dependencies.kt') }}
          restore-keys: |
            ${{ runner.os }}-konan-

      - name: Update library version
        env:
          PUBLISH_VERSION: 8.0.${{ github.run_number }}
        run: echo PUBLISH_VERSION=$PUBLISH_VERSION > ./versions.properties

      - name: Update local properties
        env:
          PUBLISH_REPO_USER: ${{ secrets.PUBLISH_REPO_USER }}
          PUBLISH_REPO_TOKEN: ${{ secrets.PUBLISH_REPO_TOKEN }}
        run: echo GITHUB_REPO_USER=$PUBLISH_REPO_USER$'\n'
          GITHUB_REPO_TOKEN=$PUBLISH_REPO_TOKEN > ./local.properties

      - name: Print versions.properties content
        run: cat ./versions.properties

      - name: Print local.properties content
        run: cat ./local.properties

      - name: Publish to GitHub Packages
        run: ./gradlew publishAllPublicationsToGithubRepository